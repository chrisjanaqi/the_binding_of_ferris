Animation(
    graph: {
        Idle(Side): Idle(Side),
        Move(Side): Move(Side),
        Attack(Side): Idle(Side),
    },
    data: {
        Idle(Side): AnimElement(
            start: 0,
            length: 4,
            priority: 0,
        ),
        Move(Side): AnimElement(
            start: 5,
            length: 4,
            priority: 0,
        ),
        Attack(Side): AnimElement(
            start: 10,
            length: 4,
            priority: 1,
        ),
        Hit(Side): AnimElement(
            start: 15,
            length: 3,
            priority: 2,
        ),
        Die(Side): AnimElement(
            start: 20,
            length: 5,
            priority: 3,
        ),
    },
    current: Idle(Side),
)